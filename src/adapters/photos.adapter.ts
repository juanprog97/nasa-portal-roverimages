import { CardDataPresentation } from '@/models';

export const photosDetailsAdapter: any = (data: any) => {
  const listPhotos: CardDataPresentation[] = [];
  console.log(data);
  return listPhotos;
};
